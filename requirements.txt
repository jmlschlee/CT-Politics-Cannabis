# Core
pydantic>=2.0
PyYAML>=6.0
duckdb>=0.10
rapidfuzz>=3.0
openpyxl>=3.1
reportlab>=4.0

# Collectors (live mode)
httpx>=0.27
sodapy>=2.2            # Socrata API (data.ct.gov)
beautifulsoup4>=4.12   # HTML parsing
tenacity>=8.2          # retries
pymupdf>=1.24          # cga.ct.gov roll-call PDF parsing (fitz)

# Web front end
streamlit>=1.30

# Interactive-portal fallback ONLY (off by default; install separately):
#   pip install playwright && python -m playwright install chromium

# Dev
pytest>=8.0
